# 订单模块接口测试说明

## 1. 测试目标

验证订单模块接口功能是否正常，包括创建订单、查看订单、确认订单、完成订单、取消订单等操作，以及订单状态流转和异常场景处理。

## 2. 测试环境

- 后端服务：`http://127.0.0.1:8080`
- 测试日期：2026-06-09
- 测试人员：尹双雨

## 3. 正常场景测试用例

### TC-ORD-001: 正常创建订单

```bash
curl -X POST http://127.0.0.1:8080/orders \
  -H "Authorization: Bearer <buyer_token>" \
  -H "Content-Type: application/json" \
  -d '{"productId":1,"remark":"test order","meetLocation":"lib"}'
```

**预期结果：** code=0, status=PENDING_CONFIRM, orderNo 生成正确

**实际结果：** PASS

### TC-ORD-002: 查看买家订单列表

```bash
curl "http://127.0.0.1:8080/orders?role=buyer&page=1&pageSize=10" \
  -H "Authorization: Bearer <buyer_token>"
```

**预期结果：** code=0, data.items 包含订单

**实际结果：** PASS

### TC-ORD-003: 查看卖家订单列表

```bash
curl "http://127.0.0.1:8080/orders?role=seller&page=1&pageSize=10" \
  -H "Authorization: Bearer <seller_token>"
```

**预期结果：** code=0, data.items 包含订单

**实际结果：** PASS

### TC-ORD-004: 按状态筛选订单列表

```bash
curl "http://127.0.0.1:8080/orders?role=buyer&status=PENDING_CONFIRM&page=1&pageSize=10" \
  -H "Authorization: Bearer <buyer_token>"
```

**预期结果：** code=0, items 只包含 PENDING_CONFIRM 状态的订单

**实际结果：** PASS

### TC-ORD-005: 查看订单详情（买家）

```bash
curl "http://127.0.0.1:8080/orders/1" \
  -H "Authorization: Bearer <buyer_token>"
```

**预期结果：** code=0, 数据完整，buyerId 与当前用户一致

**实际结果：** PASS

### TC-ORD-006: 查看订单详情（卖家）

```bash
curl "http://127.0.0.1:8080/orders/1" \
  -H "Authorization: Bearer <seller_token>"
```

**预期结果：** code=0, 数据完整，sellerId 与当前用户一致

**实际结果：** PASS

### TC-ORD-007: 卖家确认订单

```bash
curl -X POST "http://127.0.0.1:8080/orders/1/confirm" \
  -H "Authorization: Bearer <seller_token>"
```

前置条件：订单状态为 PENDING_CONFIRM

**预期结果：** code=0, status=WAIT_MEET, confirmTime 生成

**实际结果：** PASS

### TC-ORD-008: 卖家完成订单

```bash
curl -X POST "http://127.0.0.1:8080/orders/1/complete" \
  -H "Authorization: Bearer <seller_token>"
```

前置条件：订单状态为 WAIT_MEET

**预期结果：** code=0, status=COMPLETED, finishTime 生成

**实际结果：** PASS

### TC-ORD-009: 买家取消订单

```bash
curl -X POST "http://127.0.0.1:8080/orders/2/cancel" \
  -H "Authorization: Bearer <buyer_token>" \
  -H "Content-Type: application/json" \
  -d '{"reason":"不需要了"}'
```

前置条件：订单状态为 PENDING_CONFIRM

**预期结果：** code=0, status=CANCELED, cancelReason 记录正确

**实际结果：** PASS

### TC-ORD-010: 卖家取消订单

```bash
curl -X POST "http://127.0.0.1:8080/orders/3/cancel" \
  -H "Authorization: Bearer <seller_token>" \
  -H "Content-Type: application/json" \
  -d '{"reason":"商品已售出"}'
```

前置条件：订单状态为 PENDING_CONFIRM 或 WAIT_MEET

**预期结果：** code=0, status=CANCELED

**实际结果：** PASS

### TC-ORD-011: 管理员修改订单状态

```bash
curl -X PUT "http://127.0.0.1:8080/admin/orders/4/status" \
  -H "Authorization: Bearer <admin_token>" \
  -H "Content-Type: application/json" \
  -d '{"status":"CANCELED","reason":"管理员介入处理"}'
```

**预期结果：** code=0, status 更新为指定状态

**实际结果：** PASS

### TC-ORD-012: 管理员异常关闭订单

```bash
curl -X POST "http://127.0.0.1:8080/admin/orders/5/exception-close" \
  -H "Authorization: Bearer <admin_token>" \
  -H "Content-Type: application/json" \
  -d '{"reason":"双方协商不一致","responsibleParty":"BUYER"}'
```

前置条件：订单状态为 WAIT_MEET

**预期结果：** code=0, status=EXCEPTION_CLOSED

**实际结果：** PASS

## 4. 异常场景测试用例

### TC-ORD-013: 未登录创建订单

```bash
curl -X POST http://127.0.0.1:8080/orders \
  -H "Content-Type: application/json" \
  -d '{"productId":1}'
```

**预期结果：** code=401, message=unauthorized

**实际结果：** PASS

### TC-ORD-014: 重复预约同一商品

```bash
curl -X POST http://127.0.0.1:8080/orders \
  -H "Authorization: Bearer <buyer_token>" \
  -H "Content-Type: application/json" \
  -d '{"productId":1}'
```

前置条件：该用户对该商品已有进行中订单

**预期结果：** code=400, message 提示已有进行中的订单

**实际结果：** PASS

### TC-ORD-016: 商品已被锁定/售出

```bash
curl -X POST http://127.0.0.1:8080/orders \
  -H "Authorization: Bearer <other_buyer_token>" \
  -H "Content-Type: application/json" \
  -d '{"productId":1}'
```

前置条件：商品状态为 LOCKED 或 SOLD

**预期结果：** code=400, message 提示商品不可用

**实际结果：** PASS

### TC-ORD-017: 非卖家确认订单

```bash
curl -X POST "http://127.0.0.1:8080/orders/1/confirm" \
  -H "Authorization: Bearer <buyer_token>"
```

前置条件：使用买家 token 确认订单

**预期结果：** code=400 或 403, message 提示无权操作

**实际结果：** PASS

### TC-ORD-018: 确认非 PENDING_CONFIRM 状态的订单

```bash
curl -X POST "http://127.0.0.1:8080/orders/6/confirm" \
  -H "Authorization: Bearer <seller_token>"
```

前置条件：订单状态为 WAIT_MEET 或 CANCELED

**预期结果：** code=400, message 提示状态错误

**实际结果：** PASS

### TC-ORD-019: 非买卖双方查看订单详情

```bash
curl "http://127.0.0.1:8080/orders/1" \
  -H "Authorization: Bearer <other_token>"
```

前置条件：使用既不是买家也不是卖家的 token

**预期结果：** code=403, message=permission denied

**实际结果：** PASS

### TC-ORD-020: 取消已完成的订单

```bash
curl -X POST "http://127.0.0.1:8080/orders/7/cancel" \
  -H "Authorization: Bearer <buyer_token>" \
  -H "Content-Type: application/json" \
  -d '{"reason":"test"}'
```

前置条件：订单状态为 COMPLETED

**预期结果：** code=400, message 提示状态错误

**实际结果：** PASS

### TC-ORD-021: 完成未确认的订单

```bash
curl -X POST "http://127.0.0.1:8080/orders/8/complete" \
  -H "Authorization: Bearer <seller_token>"
```

前置条件：订单状态为 PENDING_CONFIRM

**预期结果：** code=400, message 提示状态错误

**实际结果：** PASS

### TC-ORD-022: 缺少取消原因

```bash
curl -X POST "http://127.0.0.1:8080/orders/1/cancel" \
  -H "Authorization: Bearer <buyer_token>" \
  -H "Content-Type: application/json" \
  -d '{}'
```

**预期结果：** code=400, message 提示取消原因必填

**实际结果：** PASS

### TC-ORD-023: 管理员异常关闭缺少责任方

```bash
curl -X POST "http://127.0.0.1:8080/admin/orders/1/exception-close" \
  -H "Authorization: Bearer <admin_token>" \
  -H "Content-Type: application/json" \
  -d '{"reason":"test"}'
```

**预期结果：** code=400, message 提示 responsibleParty 必填

**实际结果：** PASS

### TC-ORD-024: 非管理员访问管理员接口

```bash
curl -X PUT "http://127.0.0.1:8080/admin/orders/1/status" \
  -H "Authorization: Bearer <user_token>" \
  -H "Content-Type: application/json" \
  -d '{"status":"CANCELED"}'
```

前置条件：使用普通用户 token

**预期结果：** code=403, message 提示权限不足

**实际结果：** PASS

### TC-ORD-025: 查看不存在的订单

```bash
curl "http://127.0.0.1:8080/orders/99999" \
  -H "Authorization: Bearer <buyer_token>"
```

**预期结果：** code=404, message=order not found

**实际结果：** PASS

### TC-ORD-026: 传入无效订单ID

```bash
curl "http://127.0.0.1:8080/orders/abc" \
  -H "Authorization: Bearer <buyer_token>"
```

**预期结果：** code=400, message=invalid order id

**实际结果：** PASS

## 5. 测试结果汇总

| 用例编号 | 用例名称 | 结果 |
|---------|---------|------|
| TC-ORD-001 | 正常创建订单 | PASS |
| TC-ORD-002 | 查看买家订单列表 | PASS |
| TC-ORD-003 | 查看卖家订单列表 | PASS |
| TC-ORD-004 | 按状态筛选订单列表 | PASS |
| TC-ORD-005 | 查看订单详情（买家） | PASS |
| TC-ORD-006 | 查看订单详情（卖家） | PASS |
| TC-ORD-007 | 卖家确认订单 | PASS |
| TC-ORD-008 | 卖家完成订单 | PASS |
| TC-ORD-009 | 买家取消订单 | PASS |
| TC-ORD-010 | 卖家取消订单 | PASS |
| TC-ORD-011 | 管理员修改订单状态 | PASS |
| TC-ORD-012 | 管理员异常关闭订单 | PASS |
| TC-ORD-013 | 未登录创建订单 | PASS |
| TC-ORD-014 | 重复预约同一商品 | PASS |
| TC-ORD-015 | 商品已被锁定/售出 | PASS |
| TC-ORD-016 | 非卖家确认订单 | PASS |
| TC-ORD-017 | 确认非 PENDING_CONFIRM 状态的订单 | PASS |
| TC-ORD-018 | 非买卖双方查看订单详情 | PASS |
| TC-ORD-019 | 取消已完成的订单 | PASS |
| TC-ORD-020 | 完成未确认的订单 | PASS |
| TC-ORD-021 | 缺少取消原因 | PASS |
| TC-ORD-022 | 管理员异常关闭缺少责任方 | PASS |
| TC-ORD-023 | 非管理员访问管理员接口 | PASS |
| TC-ORD-024 | 查看不存在的订单 | PASS |
| TC-ORD-025 | 传入无效订单ID | PASS |
| TC-ORD-026 | 用户账号状态变更自动异常关闭订单 | PASS |

**合计：26/26 通过**

### TC-ORD-026: 用户账号状态变更自动异常关闭订单

前置条件：用户有处于 WAIT_MEET 状态的订单

**操作步骤：**

1. 管理员禁用或封禁该用户账号

**预期结果：** 

- 该用户作为买家或卖家的 WAIT_MEET 订单自动变为 EXCEPTION_CLOSED
- 管理员日志中记录：`因用户账号状态变更，系统自动异常关闭订单；责任方=买家/卖家，原因=xxx`

**实际结果：** PASS

## 6. 问题记录

无
